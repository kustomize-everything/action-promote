---
name: Testing
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  action-testing:
    name: Action Testing
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v3

      - name: Read in examples to github output
        id: read-examples
        shell: bash
        run: |-
          # Generate unique EOF value to prevent injection attack
          EOF=$(dd if=/dev/urandom bs=15 count=1 status=none | base64)

          echo "SIMPLE_PROMOTION<<$EOF" >> $GITHUB_ENV
          cat example/simple-promotion.json >> $GITHUB_ENV
          echo "$EOF" >> $GITHUB_ENV

          echo "COMPLEX_PROMOTION<<$EOF" >> $GITHUB_ENV
          cat example/complex-promotion.json >> $GITHUB_ENV
          echo "$EOF" >> $GITHUB_ENV

          echo "CROSS_OVERLAY_PROMOTION<<$EOF" >> $GITHUB_ENV
          cat example/cross-overlay-promotion.json >> $GITHUB_ENV
          echo "$EOF" >> $GITHUB_ENV

          echo "HELM_PROMOTION<<$EOF" >> $GITHUB_ENV
          cat example/helm-promotion.json >> $GITHUB_ENV
          echo "$EOF" >> $GITHUB_ENV

      - name: Checkout Testing Repo
        uses: actions/checkout@v3
        with:
          fetch-depth: 0
          repository: kustomize-everything/test-deploy
          ref: 02c8afb500ed14d507a1ac2b9df61c6a751111e2
          path: test-deploy
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Action Test with Simple Promotion
        uses: ./
        with:
          target-repo: kustomize-everything/test-deploy
          target-branch: ${{ env.BRANCH }}
          working-directory: test-deploy
          images: ${{ env.SIMPLE_PROMOTION }}
          github-token: ${{ secrets.GITHUB_TOKEN }}
          dry-run: true

      - name: Show Git Diff and Reset
        shell: bash
        working-directory: test-deploy
        run: |-
          git diff
          git reset --hard

      - name: Action Test with Simple Promotion (Inline)
        uses: ./
        with:
          target-repo: kustomize-everything/test-deploy
          target-branch: ${{ env.BRANCH }}
          working-directory: test-deploy
          images: |-
            [
              {
                "name": "nginx",
                "newName": "nginx-inline",
                "newTag": "1.9999.0",
                "overlays": ["env/dev"]
              }
            ]
          github-token: ${{ secrets.GITHUB_TOKEN }}
          dry-run: true

      - name: Show Git Diff and Reset
        shell: bash
        working-directory: test-deploy
        run: |-
          git diff
          git reset --hard

      - name: Action Test with Complex Promotion
        uses: ./
        with:
          target-repo: kustomize-everything/test-deploy
          target-branch: ${{ env.BRANCH }}
          working-directory: test-deploy
          images: ${{ env.COMPLEX_PROMOTION }}
          github-token: ${{ secrets.GITHUB_TOKEN }}
          dry-run: true

      - name: Show Git Diff and Reset
        shell: bash
        working-directory: test-deploy
        run: |-
          git diff
          git reset --hard

      - name: Action Test with Cross Overlay Promotion
        uses: ./
        with:
          target-repo: kustomize-everything/test-deploy
          target-branch: ${{ env.BRANCH }}
          working-directory: test-deploy
          images: ${{ env.CROSS_OVERLAY_PROMOTION }}
          github-token: ${{ secrets.GITHUB_TOKEN }}
          dry-run: true

      - name: Show Git Diff and Reset
        shell: bash
        working-directory: test-deploy
        run: |-
          git diff
          git reset --hard

      - name: Action Test with Helm Promotion
        uses: ./
        with:
          target-repo: kustomize-everything/test-deploy
          target-branch: ${{ env.BRANCH }}
          working-directory: test-deploy
          charts: ${{ env.HELM_PROMOTION }}
          github-token: ${{ secrets.GITHUB_TOKEN }}
          dry-run: true

  python-tests:
    name: Python Tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v3

      - name: Setup Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.9

      - name: Install Dependencies
        working-directory: src
        run: |-
          pip install -r requirements.txt
          poetry install

      - name: Run Tests
        working-directory: src
        run: |-
          poetry run python -m unittest tests/*_test.py
